# data/samples/

A small, committed slice of the dataset (a handful of buildings, a few days)
used by unit/integration tests and local demos so nobody needs to run the
full download just to run the test suite. Populated in Phase 4.
