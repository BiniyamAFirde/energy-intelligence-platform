# data/raw/

Exact, untouched files downloaded from the official BDG2 repository by
`scripts/download_data.py` (Phase 4). Nothing in this pipeline ever writes
into this folder except the download script. Gitignored — populate it by
running the download script, not by committing files here.
